import { Layout } from '@components/layout/Layout';
import { COMPANYNAME } from 'company';
import React from 'react';

export const ContactUsPage = () => {
    return <Layout pageTitle={`${COMPANYNAME} - Contact Us`}>Contact Us page!</Layout>;
};
