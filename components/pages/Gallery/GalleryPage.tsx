import { Layout } from '@components/layout/Layout';
import { COMPANYNAME } from 'company';
import React from 'react';

export const GalleryPage = () => {
    return <Layout pageTitle={`${COMPANYNAME} - Gallery`}>Gallery page!</Layout>;
};
