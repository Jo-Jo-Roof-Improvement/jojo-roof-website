import { Layout } from '@components/layout/Layout';
import { COMPANYNAME } from 'company';
import React from 'react';

export const ServicesPage = () => {
    return <Layout pageTitle={`${COMPANYNAME} - Services`}>Services page!</Layout>;
};
